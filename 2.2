#include <stdio.h>
int  fun(int a[])
{
	int i,b;
	for (i = 1; i <= 1000; i++)
	{
		if (i % 7 == 0 || i % 11 == 0)
			if (i % 77 != 0)
				a[b++] = i;
		/*
		for (int i = 0 ; i < 1000 ; i++ )
		{
        if(i%7==0)
            if(i%11!=0)
                a[b++]=i;
        if(i%11==0)
            if(i%7!=0)
                a[b++]=i;
		}
		*/	
	}
	return b;
}
int main()
{
	int aa[1000], n, k;
	n = fun(aa);
	for (k = 0; k < n; k++)
		if ((k + 1) % 10 == 0)
			printf("\n");
		else
			printf("%5d", aa[k]);
	return 0;
}
